<%= form_for(@cualquiera) do |f| %>
  <% if @cualquiera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cualquiera.errors.count, "error") %> prohibited this cualquiera from being saved:</h2>

      <ul>
      <% @cualquiera.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :columna1 %><br>
    <%= f.text_field :columna1 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
